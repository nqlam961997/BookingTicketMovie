const initialState = {};

export const QuanLyPhimReducer = (state = initialState, action) => {
  switch (action.type) {
    // case typeName:
    //   return { ...state, ...payload }

    default:
      return state;
  }
};
